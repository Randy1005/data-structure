1-1
I used two ways to complete the function, one by for-loop(iter_factorial), and the other one by recursive algorithm(rec_factorial).

1-2
I use the "Backtracking" algorithm to finish the assignment. But the output is not formatted.